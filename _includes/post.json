{
  "id"    : "{{ site.baseurl }}{{ post.url }}",
  "title"   : "{{ post.title }}",
  "content" : "{{ post.content | strip_html | escape | strip_newlines | remove:'"' }}",
  "description" : "{{ post.description }}"
},
